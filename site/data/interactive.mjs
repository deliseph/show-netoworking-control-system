// Everything the prose cannot supply.
//
// The drill cards, the myth deck and the glossary are all generated from the
// markdown at build time, so they cannot drift from what is taught. This file
// holds only what has no source in the prose: a correct answer, a distractor, a
// model answer, and a fault with a cost attached to each way of chasing it.

// ---------------------------------------------------------------------------
// The Five Questions
//
// The recurring model of the whole module. Every protocol answers all five, and
// the fifth is the one nobody documents.
// ---------------------------------------------------------------------------

export const questionMeta = {
  message: {
    label: '1 · What is a message?',
    hint: 'Where does one start and stop, how big is it, and what is inside it?',
  },
  address: {
    label: '2 · Who is it for?',
    hint: 'Everybody, one device, a group, or nobody in particular?',
  },
  deadline: {
    label: '3 · When must it arrive?',
    hint: 'A hard deadline, a soft one, or whenever it gets there?',
  },
  feedback: {
    label: '4 · How do you know it arrived?',
    hint: 'Acknowledged, repeated until it must have, or never confirmed at all?',
  },
  failure: {
    label: '5 · What happens when it does not?',
    hint: 'The one that is documented in a sentence or not at all.',
  },
};

// Sort these into the question they are an answer to. The point of the exercise
// is that the same protocol appears under several, and that reading the fifth
// answers is uncomfortable.
export const questionCards = [
  { t: 'A DMX break, then a start code, then up to 512 slots', q: 'message', why: 'The break is the frame marker: the line held low longer than any valid byte can be.' },
  { t: 'A UDP datagram on port 5568 carrying one universe', q: 'message', why: 'sACN puts one universe in one packet, deliberately inside a single Ethernet frame.' },
  { t: 'F0 7F 01 02 01 01 ... F7', q: 'message', why: 'A System Exclusive message. F0 opens it, F7 closes it, and MSC lives in between.' },
  { t: 'A string beginning with a slash, a comma-led type tag, then arguments', q: 'message', why: 'An OSC message. Everything padded to a multiple of four bytes.' },
  { t: '80 bits per video frame, biphase mark encoded onto an audio track', q: 'message', why: 'LTC. Self-clocking, polarity independent, readable at any speed.' },
  { t: 'A status byte with the top bit set, then data bytes with it clear', q: 'message', why: 'MIDI. The top bit is what distinguishes the two, which is why data stops at 127.' },

  { t: 'Nobody. Every device hears every byte and picks out its own', q: 'address', why: 'DMX512 has no addressing in the protocol at all. The address is a setting on the fixture.' },
  { t: 'A multicast group, one per universe, in 239.255.0.0/16', q: 'address', why: 'sACN. The switch delivers it only to devices that joined that group, if snooping works.' },
  { t: 'Broadcast, by default, to every device on the network', q: 'address', why: 'Art-Net. Unicast and multicast are available and most rigs never turn them on.' },
  { t: 'A 48-bit UID: 16 bits of manufacturer, 32 of device', q: 'address', why: 'RDM. Discovered by binary search over the whole address space.' },
  { t: 'A device ID from 0 to 127, where 127 means everybody', q: 'address', why: 'MIDI Show Control. Plus a command format naming the discipline.' },
  { t: 'An address and a port together', q: 'address', why: 'A socket. The address gets you to the machine, the port to the program.' },

  { t: 'Continuously, about every 22.7 milliseconds', q: 'deadline', why: 'A full DMX universe. Fewer channels refresh faster, which is a real effect on fast chases.' },
  { t: 'Within a quarter of a millisecond, or the audio clicks', q: 'deadline', why: 'Dante and AES67 at their tightest latency setting.' },
  { t: 'Within a frame, or the picture tears', q: 'deadline', why: 'Video over IP. ST 2110 is sub-frame; NDI trades one to three frames for convenience.' },
  { t: 'Whenever. It is a discovery scan and nothing is waiting for it', q: 'deadline', why: 'Management traffic. No deadline, therefore no manners, which is why it gets its own VLAN.' },
  { t: 'Sub-microsecond, or every downstream device drifts', q: 'deadline', why: 'PTP. This is why it needs one grandmaster, one profile and no router in the path.' },
  { t: 'Loose: twenty to fifty milliseconds is fine', q: 'deadline', why: 'A cue. Its latency requirement is loose and its loss consequence is catastrophic, which is the opposite shape from audio.' },

  { t: 'You do not. There is no return path of any kind', q: 'feedback', why: 'DMX512 is simplex. Everything you believe about the rig, you believe because you sent it.' },
  { t: 'You do not, but the whole picture repeats forty four times a second', q: 'feedback', why: 'This is how DMX and sACN get reliability without acknowledgement.' },
  { t: 'A response message, with a checksum, in the gap after the request', q: 'feedback', why: 'RDM. Half duplex on the same pair, and the turnaround timing is why splitters break it.' },
  { t: 'A three-way handshake, sequence numbers and retransmission', q: 'feedback', why: 'TCP. Right for a command that must not be lost, wrong for streaming data.' },
  { t: 'An HTTP status code', q: 'feedback', why: 'The only common cue path that actually tells you whether it worked.' },
  { t: 'A keep-alive byte every 300 milliseconds', q: 'feedback', why: 'MIDI active sensing. A 1983 protocol answering the fourth question better than most modern ones.' },

  { t: 'The fixture holds its last look, fades, or goes dark: a menu setting', q: 'failure', why: 'DMX and sACN. Find out which, for the actual fixtures, and write it on the drawing.' },
  { t: 'The cue simply never happens, and nothing anywhere reports it', q: 'failure', why: 'MSC over DIN, and OSC over UDP. Fire and forget.' },
  { t: 'The source times out after two and a half seconds, then the device decides', q: 'failure', why: 'sACN. The timeout is in the standard; what happens next is not.' },
  { t: 'The amplifier mutes after a few seconds of no clock', q: 'failure', why: 'Audio over IP. Losing PTP is losing the audio, not merely losing sync.' },
  { t: 'The machine stops and holds, because that is the only safe answer', q: 'failure', why: 'Machinery. Which is why its safety function is not on this network at all.' },
  { t: 'Everything works for four minutes, then multicast floods every port', q: 'failure', why: 'IGMP snooping with no querier. Memberships expire and the switch reverts to flooding.' },
  { t: 'The video freewheels, then stops, and the stage manager calls it manually', q: 'failure', why: 'Lost timecode. This is why the fallback belongs in the failure table.' },
  { t: 'Two devices answer to one address and the fault moves every reboot', q: 'failure', why: 'A duplicate IP address, or two DHCP servers. It shows in the ARP table first.' },
];

// ---------------------------------------------------------------------------
// Self test: model answers, per session
// ---------------------------------------------------------------------------

export const selfTest = {
  1: [
    { q: 'A producer asks why the lighting console cannot simply "talk to" the video server, since both are on the network. Answer in two sentences.',
      a: 'Sharing a cable is not sharing an agreement. Both devices can put packets on the same wire, and unless they implement the same protocol with the same message format, addressing and vocabulary, they are exactly as incompatible as they were before, plus they can now congest each other.' },
    { q: 'Name the three control postures and give the type example and the failure signature of each.',
      a: 'State-based: the controller continuously says what the world should look like now. Lighting over DMX, repeated at 44 Hz. A lost packet self-heals in 22 ms. Event-based: the controller says do this now and then says nothing. MSC. A lost message means the cue never happened and nothing knows. Streamed: a continuous flow that must arrive in order and on time. Audio and video. A lost packet is a click or a dropped frame, and lateness is as bad as loss.' },
    { q: 'What is the difference between feedback and telemetry, and why does it matter in a production meeting?',
      a: 'Feedback is coupled to control: the system uses the returned information to act. Telemetry is reported state that a human reads and nothing acts on. Most of what a modern rig reports, RDM sensor values, a Dante device dropping off, a media server out of disc, is telemetry. It matters because saying "the system monitors itself" implies something will happen automatically, and usually nothing will.' },
    { q: 'Why does a centralised architecture failing completely have an advantage over a distributed one failing partially?',
      a: 'Because it is obvious. One box stops, the show stops, and everybody knows why within ten seconds, so the rehearsed changeover to the spare begins immediately. A distributed system loses three of eight zones and the diagnosis takes an hour the show does not have. The trade is degradation against clarity, and which one you want depends on whether there is a technician per zone.' },
    { q: 'State the Five Questions, and say which one is usually missing from a manual.',
      a: 'What is a message, who is it for, when must it arrive, how do you know it arrived, and what happens when it does not. The fifth. Almost every protocol is specified in detail for the case where everything works and in one sentence or not at all for the case where it stops, and that sentence is the one a production manager actually needs.' },
  ],
  2: [
    { q: 'A GPO on a lighting console is wired to a GPI on a playback machine and nothing happens. The wiring is continuous. What is the most likely cause?',
      a: 'Both ends are dry, or both are wet. A dry contact has no voltage of its own and the receiver must supply it; a wet output supplies its own. Dry to dry means nobody supplies the voltage and nothing happens at all, which looks like a fault rather than like a category error. Read both manuals and look for the words volt-free, dry contact, sourcing and sinking.' },
    { q: 'Why is a Form C contact worth more than a Form A contact on a show-critical GPO?',
      a: 'Form C is a changeover: common, normally open and normally closed. That lets the receiving end distinguish activated from disconnected. With a Form A contact a cut cable and an inactive output look identical, forever, and the first time you find out is when a cue does not fire.' },
    { q: 'You have specified a 5 kV optocoupler between the control side and the load side. Give two ways the isolation can be zero anyway.',
      a: 'Any conductive path around the outside of the component: a shared power supply ground between the two sides, a mounting screw or rack rail bridging both ground planes, a cable screen carrying the two grounds together on a parallel route, or a bench earth connected during commissioning and left. Isolation is a property of the whole system, and the test is resistance between the two grounds with everything connected. It should read open.' },
    { q: 'Convert 0xB7 to binary and to decimal, and say what the top bit would mean if this were a MIDI byte.',
      a: '0xB7 is 1011 0111, which is 128 + 32 + 16 + 4 + 2 + 1 = 183. Split it as two nibbles: B is 1011 and 7 is 0111. The top bit is set, so as MIDI this is a status byte, specifically 0xBn, Control Change, on channel 8 since the low nibble is 7 and channels are displayed one higher.' },
    { q: 'A colleague proposes putting the emergency stop on the show network because it will be faster and easier to wire. Give the argument against in three sentences.',
      a: 'A safety function must work when everything else has failed, and a shared best-effort network is least trustworthy exactly then: congested, misconfigured, or full of discovery traffic. Safety functions are hardwired dual-channel with monitoring, or on a certified safety bus, so that a single fault such as a broken wire or a welded contact is detected rather than ignored. It is also not an engineering preference: it is a certification requirement under EN ISO 13849 or IEC 62061, and for stage machinery in Europe, EN 17206.' },
  ],
  3: [
    { q: 'Explain what layering buys you when something is broken, rather than what the seven layers are called.',
      a: 'Each layer depends only on the one below it, so if the layer below is proved good the fault is at or above where you are standing. That turns a vague symptom into a sequence of tests that each eliminate half the system: link light, then MAC, then IP, then port. It is a search strategy, not a diagram to memorise.' },
    { q: 'A serial link produces continuous data that is valid bytes and complete nonsense. What is the fault and why does it look like this?',
      a: 'The baud rate is wrong at one end. There is no clock line: the receiver uses the start bit edge to synchronise and then samples in the middle of each bit at the rate it was told. At the wrong rate it samples at the wrong moments and assembles bytes that are technically valid values and meaningless. A wrong rate gives garbage rather than nothing, which is why "there is data but it is nonsense" is a rate problem far more often than a wiring one.' },
    { q: 'Calculate the network load of one sACN universe, and say why the answer is not 200 kbit/s.',
      a: 'About 638 bytes of sACN plus roughly 42 bytes of Ethernet, IP and UDP headers is 680 bytes. At 44 packets per second: 680 x 8 x 44 = 239,360, so about 240 kbit/s. The 200 kbit/s figure is the DMX payload alone; the headers are 20 per cent on top, and across a hundred universes that difference is 4 Mbit/s.' },
    { q: 'Why does detecting an error not help a sACN packet, and why does that not matter?',
      a: 'Ethernet detects a corrupted frame with its CRC and silently drops it. Nothing retransmits unless a higher layer asks, and UDP does not ask, so the packet is simply gone. It does not matter because sACN sends the complete picture again 22 milliseconds later, so the loss is self-repairing. The same loss on an OSC cue is a cue that never happened, which is why the two need different handling.' },
    { q: 'Give two reasons other than distance to choose fibre inside a venue.',
      a: 'Complete electrical isolation, so there is no ground path between the two ends and no shared fault path, which removes a whole category of hum and of equipment damage. And total immunity to the electromagnetic environment created by dimmers, motors and LED drivers, which is exactly the environment a venue is.' },
  ],
  4: [
    { q: 'A switch receives a frame for a MAC address it has never seen. What does it do, and what does that imply about a silent device?',
      a: 'It floods the frame out every port except the one it arrived on, because it has no entry to forward it by. Table entries age out after about 300 seconds of silence, so a device that has said nothing for five minutes has its traffic flooded to the whole network when somebody talks to it again. Silent devices are noisier on a network than chatty ones, which is unintuitive and true.' },
    { q: 'Given 10.101.7.150/26, produce the network, the usable range and the broadcast, and show the method.',
      a: 'A /26 has a mask of 255.255.255.192, so the block size is 256 minus 192 = 64. Networks fall at .0, .64, .128 and .192. 150 divided by 64 is 2.34, so round down to 2: the third block, starting at .128. Network 10.101.7.128, usable .129 to .190, broadcast .191. Six host bits gives 64 minus 2 = 62 usable addresses.' },
    { q: 'A device shows an address of 169.254.31.7. What has happened, and what are the three likely causes in order?',
      a: 'It is set to DHCP, it asked, and nothing answered, so it gave itself a link-local address. In order: there is no DHCP server on that network; the link between the device and the server is broken; or a VLAN misconfiguration has put the device somewhere the server is not. Two devices that both fall back can often still talk to each other, which is why half a rig can appear to work while the console sees none of it.' },
    { q: 'Why do sACN, Dante and ST 2110 all use UDP when TCP is more reliable?',
      a: 'Because for streaming data retransmission is worse than loss. A lighting level from 40 milliseconds ago is not worth having: a fresh one is arriving now. An audio sample that arrives late is a click either way, and TCP would also slow the whole stream down while it retried. These protocols handle loss by repeating the current state, not by asking again for the old one.' },
    { q: 'Somebody proposes putting the crew Wi-Fi on the same network as the sACN so the remote focus app works. What do you say?',
      a: 'Put the remote on its own VLAN with an access list to reach only what it needs. The show network must not carry untrusted devices, because none of the show protocols authenticate anything: anything on that VLAN can send sACN at priority 200 and own the rig. Separately, a human holding a tablet is a fine use of Wi-Fi, because a human notices a failure; a cue path is not, because you do not control the medium.' },
  ],
  5: [
    { q: 'A rig is proved working at 14:00 and degrades during the show at 20:15, with multicast reaching every port. Name the fault and the fix.',
      a: 'IGMP snooping is enabled but there is no querier. Group memberships expire after about 260 to 300 seconds and the switch reverts to flooding, so it works while you are actively testing and fails once it has been left alone. The fix is exactly one querier per VLAN, usually the core switch. The test that would have caught it is to prove the rig, leave it untouched for ten minutes, and prove it again.' },
    { q: 'Why is RSTP the wrong redundancy mechanism for Dante, and what is the right one?',
      a: 'RSTP reconverges in one to six seconds, which is fine for lighting and is several seconds of dropped audio. The right mechanism is two physically separate networks with identical streams on both and the receiver taking whichever arrives, which is what Dante redundancy, ST 2110-7 and SMPTE 2022-7 all do. There is no convergence time because there is no reconvergence: both paths were already live.' },
    { q: 'Audio glitches at irregular intervals with no other symptom and no configuration change. Where do you look first and why?',
      a: 'PTP. Filter for it in Wireshark and count distinct grandmaster identities in the announce messages. Two devices with the same priority will flap between being grandmaster, and the resulting clock steps are exactly this symptom. Also check that PTP is not crossing a router, which adds asymmetric delay, and that every device is on the same profile and domain.' },
    { q: 'Explain why a VLAN is not a firewall, in the context of a show network.',
      a: 'A VLAN separates broadcast domains: devices in different VLANs cannot reach each other at layer 2. But if the VLANs are routed, which they usually are so that management and control can reach things, then traffic crosses freely unless you filter it. VLANs give you the place to put a filter and the containment of broadcast; they do not themselves inspect or block anything.' },
    { q: 'You inherit a show network with no documentation. Give five checks that take twenty minutes and say what each proves.',
      a: 'Wireshark protocol hierarchy, for how much is broadcast and multicast. Filter igmp, for whether a querier exists at all. Filter dhcp and count who answers an offer, because more than one server is an emergency. Filter ptp and count grandmaster identities, because a flapping election is the audio fault. And read the switch port counters, because rising CRC errors are a physical fault that no configuration will fix.' },
  ],
  6: [
    { q: 'A rig behaves strangely: colours respond to the gobo wheel and everything is out by a few channels. What is the fault?',
      a: 'A personality or mode mismatch. The fixture is in a different mode from the one the console is patched to, so its footprint and channel order differ and every parameter after the first divergence is offset. Check the fixture menu against the console patch, not the address, because the address is probably right.' },
    { q: 'Calculate the sACN multicast group for universe 300, and say why that arithmetic matters operationally.',
      a: '300 divided by 256 is 1 remainder 44, so the group is 239.255.1.44. It matters because it is how you write a Wireshark filter that isolates one universe out of a hundred, and because it is what the switch is actually building its IGMP table from: a device joins that group and the switch forwards that group to that port, and nothing else.' },
    { q: 'RDM discovers no fixtures although all of them respond to DMX perfectly. What is the most likely cause and why does the symptom mislead?',
      a: 'Something in the path does not pass the return direction: usually a splitter that is not RDM-capable, or a node with RDM disabled. DMX flows perfectly because that direction is unaffected, so every fixture works and none is discoverable. The symptom points at the fixtures and the cause is a box in the middle, which is why this wastes so much time.' },
    { q: 'Explain the argument for terminating a DMX line that currently works without a terminator.',
      a: 'Without a terminator the reflection is present and is merely small enough to survive today. Add twenty metres, add a fixture, change the temperature, and it becomes intermittent flicker somewhere apparently unrelated to what you changed. Termination is not about whether it works now, it is about whether it still works in three weeks with a different rig on the end.' },
    { q: 'Why is sACN priority 200 the wrong default for a main console?',
      a: 'Because nothing can then override you, including the person trying to fix a problem you caused, and including a legitimate override tool. The convention that works is default 100 for the main console, lower, around 90, for a backup sending the same universes continuously so it takes over automatically, and higher for a focus or test tool that takes control deliberately and releases by stopping.' },
  ],
  7: [
    { q: 'Why do MIDI data values stop at 127, and what single test distinguishes a status byte from a data byte?',
      a: 'The top bit of every MIDI byte says which kind of byte it is: set means status, clear means data. That spends one bit of the eight, leaving seven for data, so 0 to 127. The test is byte AND 0x80: non-zero means status. Once you know that rule the whole protocol becomes readable.' },
    { q: 'Build the MSC message for GO cue 7 on lighting, device 3, and explain each byte.',
      a: 'F0 7F 03 02 01 01 37 F7. F0 opens the SysEx. 7F is universal real-time. 03 is the device ID. 02 is the sub-ID meaning this is MSC. 01 is the command format, lighting. 01 is the command, GO. 37 is ASCII "7", the cue number. F7 closes it. Omit the cue number entirely and it means the next cue.' },
    { q: 'A production wants to know whether a cue actually fired. Which of MSC, OSC over UDP and an HTTP request can tell them, and what does that imply?',
      a: 'Only the HTTP request, which returns a status code. MSC over DIN is fire and forget and effectively nothing implements its confirmed mode. OSC over UDP has no acknowledgement, and OSC over TCP confirms delivery to the socket rather than execution of the cue. So if confirmation matters, either use HTTP for anything not tightly timed, or build a return path yourself.' },
    { q: 'Give two genuine advantages DIN MIDI still has over USB-MIDI in a venue.',
      a: 'It is opto-isolated by specification, so a MIDI link between a lighting console and a sound rack introduces no ground path, which is more than can be said for USB or often for Ethernet. And its device identity is a physical cable rather than an enumerated USB port, so it cannot come back after a power glitch as a different port name that show software has now lost.' },
    { q: 'Somebody says "both devices speak OSC so the integration is trivial". What is wrong with that?',
      a: 'OSC specifies an encoding and a transport, not a vocabulary. There is no registry, no assigned port and no discovery, so every product invents its own address scheme: QLab uses /cue/12.5/start on 53000, Eos uses /eos/cue/1/5/fire on 3032, and neither is wrong or compatible. Every OSC integration is bespoke, and its deliverable is a documented address list kept with the show file.' },
  ],
  8: [
    { q: 'Explain drop-frame timecode to somebody who thinks it deletes frames.',
      a: 'It skips frame numbers, not frames. NTSC colour runs at 30 x 1000/1001 = 29.97 frames per second, so counting 30 numbers per second makes the timecode clock fall about 3.6 seconds behind the wall clock every hour. Drop frame skips numbers 00 and 01 at the start of every minute except every tenth minute, which is 108 numbers an hour, almost exactly the error. Every frame of the recording is present, the way a building skips a thirteenth floor.' },
    { q: 'A show at 25 fps is offered a drop-frame timecode setting. What do you say?',
      a: 'There is no drop frame at 25 fps. 25 is exactly 25, the error drop frame corrects does not exist, and a device offering it has been misconfigured. If somebody has set it, find out what else on that device was set at the same time.' },
    { q: 'Why is MTC inherently two frames late, and when does that matter?',
      a: 'A timecode value is eight nibbles, and MTC sends one nibble per quarter frame in a two-byte message, so a complete value takes eight quarter-frame messages, which is two frames. A good implementation compensates for the known offset; not all do. It matters when an MTC-driven cue has to land against an LTC-driven one, where it will sit two frames late, and it does not matter at all when nothing else is running against the same moment.' },
    { q: 'Give the three things that must be true for OSC bundle time tags to be useful, and say which one usually is not.',
      a: 'The sender must set a real time tag rather than the immediate value; the receiver must implement scheduled execution rather than acting on arrival; and both must share a clock accurate enough for the scheduling to mean anything. The second is the one that usually is not true: most implementations only support the time tag meaning immediately, so test it on the actual devices before designing around it.' },
    { q: 'A themed attraction integrator proposes putting the ride stop on the same network as the show control, arguing that it is a separate VLAN. Respond.',
      a: 'No. A VLAN separates traffic on a shared, best-effort infrastructure; it does not make that infrastructure trustworthy when everything else has failed, which is exactly when a safety function must work. A ride stop is a safety function designed to a performance level under EN ISO 13849 or a safety integrity level under IEC 62061, on hardwired dual channels or a certified safety bus, and it is not ours to design. The show network may send a cue and read status, and neither path may be able to cause motion the safety system would not permit. Draw that boundary on the system drawing.' },
  ],
};

// ---------------------------------------------------------------------------
// Readiness checks
//
// Each one tests what the session ASSUMES, never what it teaches, and every
// wrong answer names the one thing to go and fix rather than reporting a score.
// `to` is resolved at build time into a link that is checked against the pages
// actually generated, so a pointer can never dangle.
// ---------------------------------------------------------------------------

export const readiness = {
  1: [
    { q: '2 to the power of 8 is:', options: ['16', '64', '256', '512'], correct: 2,
      why: 'Eight bits have 256 possible patterns, which is why a byte holds 0 to 255.',
      fix: 'Learn the powers of two to 1024 as a lookup rather than a calculation. Every number in this module is built from them.',
      to: { route: '/foundations', id: 'powers-of-two' } },
    { q: 'In live entertainment, "production" means:', options: ['Software releases', 'Making a film', 'A live show and everything that mounts it', 'A factory process'], correct: 2,
      why: 'This module is entirely about live performance: theatre, arena, themed entertainment, events.',
      fix: 'Nothing to fix. It is here because the word is used differently in other industries and the confusion is real.' },
    { q: 'A cue is best defined as:', options: ['A button on a console', 'A change, at a moment, that somebody is responsible for', 'A lighting state', 'A file on a playback machine'], correct: 1,
      why: 'All three parts matter. A change with no moment is a preset, and a moment with nobody responsible is an automation.',
      fix: 'Read the section on cues before you arrive. The distinction between a cue and the device receiving it runs through the whole module.',
      to: { route: '/session/1', id: 'cues' } },
    { q: 'The most useful question to ask about any control device is:', options: ['What protocol does it speak?', 'What does it do when it loses its control input?', 'How fast is it?', 'What does it cost?'], correct: 1,
      why: 'It is the fifth of the Five Questions and it is the one almost nobody asks, including most manuals.',
      fix: 'Go and ask one operator what their equipment does when the control signal stops. Not what they do: what it does.' },
    { q: 'Before Session 1 you should have:', options: ['Learned DMX', 'Bought a switch', 'Nothing: it assumes only arithmetic', 'Read the whole book'], correct: 2,
      why: 'Session 1 is the opening. Everything after it assumes Session 1, which is why it is published even though it has already run.',
      fix: 'Nothing to fix. Bring a notebook you will keep for the whole module.' },
  ],
  2: [
    { q: '0xB7 in binary is:', options: ['1011 0111', '1101 0111', '1011 1011', '0111 1011'], correct: 0,
      why: 'Two nibbles: B is 1011 and 7 is 0111. One hex digit is exactly four bits, so it is a lookup rather than arithmetic.',
      fix: 'Learn the sixteen hex-to-binary rows. This session decodes bytes in front of you and it does not wait.',
      to: { route: '/foundations', id: 'hexadecimal' } },
    { q: 'One byte can hold how many different values?', options: ['8', '128', '255', '256'], correct: 3,
      why: '2 to the power of 8 is 256, which is the values 0 to 255. The commonest slip is saying 255, which is the largest value rather than the count.',
      fix: 'Spend ten minutes on bits and bytes. The difference between the count and the largest value causes real off-by-one errors.',
      to: { route: '/foundations', id: 'bits-and-bytes' } },
    { q: 'A circuit needs, at minimum:', options: ['A voltage', 'A complete path back to the source', 'A resistor', 'A ground'], correct: 1,
      why: 'Current flows only in a complete loop. This is assumed in every contact-closure and isolation discussion in the session.',
      fix: 'If this is genuinely new, the Electronics for Theatre module covers it properly. For this session, knowing that a circuit is a loop is enough.' },
    { q: 'What does 4.7 kΩ mean?', options: ['4.7 ohms', '47 ohms', '4700 ohms', '470,000 ohms'], correct: 2,
      why: 'k is a thousand. Reading prefixes fluently is assumed everywhere in this module, in ohms, bits per second and seconds.',
      fix: 'Ten minutes on the prefix table. Every value in this course is written with one.',
      to: { route: '/foundations', id: 'time-and-the-prefixes-below-one' } },
    { q: 'What should you bring to Session 2?', options: ['A soldering iron', 'A laptop, and a meter if you own one', 'A console', 'Nothing'], correct: 1,
      why: 'Half the session is on kit and there is no way to catch up during it.',
      fix: 'Read the preparation block. Also find a GPI port on real equipment and note whether it wants a dry contact or a voltage.' },
  ],
  3: [
    { q: 'Converting 0x7F to decimal gives:', options: ['79', '112', '127', '255'], correct: 2,
      why: '7 times 16 plus 15 is 127. This is the largest MIDI data byte and it will appear repeatedly.',
      fix: 'Drill hex to decimal until it takes five seconds. This session decodes bytes on a scope.',
      to: { route: '/foundations', id: 'hexadecimal' } },
    { q: 'A signal travels down copper at roughly:', options: ['The speed of light', 'Two thirds the speed of light', 'The speed of sound', 'It is instantaneous'], correct: 1,
      why: 'About 5 nanoseconds per metre. It is how you locate a cable fault from the timing of its reflection.',
      fix: 'Read the physical layer section. The lab measures a reflection and calculates a cable length from it.',
      to: { route: '/session/3', id: 'getting-a-signal-down-a-physical-thing' } },
    { q: '250 kbit/s means one bit takes:', options: ['4 ns', '4 µs', '4 ms', '250 µs'], correct: 1,
      why: 'One divided by 250,000 is 4 microseconds. This is DMX, and it is the arithmetic behind every packet timing in the module.',
      fix: 'Practise reciprocals of rates. A frequency and a period are reciprocals, and it is one division.',
      to: { route: '/foundations', id: 'reading-a-rate' } },
    { q: 'A 100 Mbit/s link can move a file at about:', options: ['100 MB/s', '12.5 MB/s', '800 MB/s', '10 MB/s'], correct: 1,
      why: 'Rates are in bits and file sizes are in bytes. Divide by eight. This is the commonest arithmetic error in the subject.',
      fix: 'Read the unit trap section. When a number looks eight times wrong, it is this.',
      to: { route: '/foundations', id: 'bits-and-bytes' } },
    { q: 'What must you bring to Session 3?', options: ['A DMX terminator', 'Laptop, and a logic analyser or pocket scope if you own one', 'A switch', 'A console'], correct: 1,
      why: 'Two of the blocks are on test equipment and there are not enough shared units.',
      fix: 'Read the preparation block, and photograph every different data connector you can find in one venue.' },
  ],
  4: [
    { q: '192.168.1.50 AND 255.255.255.0 gives:', options: ['192.168.1.50', '192.168.1.0', '255.255.255.50', '0.0.0.50'], correct: 1,
      why: 'Where the mask has a 1 the address bit survives; where it has a 0 the bit is zeroed. That AND is the whole of subnetting.',
      fix: 'This session assumes the AND is automatic. Do the subnetting trainer for twenty minutes a night for at least four nights before you arrive.',
      to: { route: '/foundations', id: 'bitwise-and-which-is-all-of-subnetting' } },
    { q: 'How many usable host addresses does a /24 have?', options: ['254', '255', '256', '253'], correct: 0,
      why: 'Total is 256; the all-zeros host is the network and the all-ones host is the broadcast, so 254 are assignable.',
      fix: 'Learn the CIDR table, and specifically learn that usable is always two fewer than total.',
      to: { route: '/field', id: 'subnetting-in-the-venue' } },
    { q: 'The block size for a /26 is:', options: ['26', '32', '64', '128'], correct: 2,
      why: 'A /26 mask octet is 192, and 256 minus 192 is 64. So the networks fall at .0, .64, .128 and .192.',
      fix: 'The block-size shortcut is what makes subnetting fast enough to do in a venue. Drill it.',
      to: { route: '/foundations', id: 'bitwise-and-which-is-all-of-subnetting' } },
    { q: 'A device showing 169.254.8.22 is telling you:', options: ['It has a static address', 'It is on a private network', 'DHCP failed', 'It is broadcasting'], correct: 2,
      why: 'Link-local is what a device gives itself when it asked for an address and nothing answered. It is a diagnosis, not a configuration.',
      fix: 'Run ipconfig or ip addr on your own machine before you arrive and know what each field means.',
      to: { route: '/field', id: 'reading-your-own-configuration' } },
    { q: 'What must you bring to Session 4?', options: ['Nothing', 'A laptop with Wireshark already installed and a tested Ethernet connection', 'A console', 'A DMX cable'], correct: 1,
      why: 'Both lab blocks are on the network, and a USB-C adapter you have not tested is the classic way to lose thirty minutes.',
      fix: 'Install Wireshark, capture sixty seconds of any network, and find one ARP packet and one DHCP packet using the filter box.' },
  ],
  5: [
    { q: 'Given 10.101.7.150/26, the network address is:', options: ['10.101.7.0', '10.101.7.64', '10.101.7.128', '10.101.7.150'], correct: 2,
      why: 'Block size 64. 150 divided by 64 is 2.34, round down to 2, so the third block starting at .128. Range .129 to .190, broadcast .191.',
      fix: 'Session 5 assumes this takes under a minute. Another four evenings on the subnetting trainer before you arrive.',
      to: { route: '/session/4', id: 'the-32-bits' } },
    { q: 'A switch receiving a frame for a MAC it has never seen will:', options: ['Drop it', 'Flood it out every port except the one it came in on', 'Send it to the gateway', 'Ask the router'], correct: 1,
      why: 'It has no entry to forward by, so it floods. Entries age out after about 300 seconds, which is why silent devices generate flooding.',
      fix: 'Reread what a switch actually does. The whole of the multicast material builds on it.',
      to: { route: '/session/4', id: 'what-a-switch-actually-does' } },
    { q: 'Multicast on a switch with no IGMP snooping behaves like:', options: ['Unicast', 'Broadcast', 'It is dropped', 'It is queued'], correct: 1,
      why: 'Without snooping the switch has no idea who wants which group, so it floods. All of sACN’s network advantage disappears.',
      fix: 'Read the addressing section of Session 4 and come with the question you want answered.',
      to: { route: '/session/4', id: 'what-a-switch-actually-does' } },
    { q: 'sACN and Dante both rely on which network service being configured correctly?', options: ['DNS', 'IGMP', 'NTP', 'DHCP'], correct: 1,
      why: 'Both are multicast. IGMP snooping decides whether the switch delivers a group only where it is wanted, and a querier keeps the memberships alive.',
      fix: 'Get into a managed switch’s web interface before you arrive and find the VLAN, IGMP and port statistics pages.' },
    { q: 'What must you bring to Session 5?', options: ['A DMX terminator', 'Laptop with Wireshark, patch cables, and your Session 4 address plan', 'A fixture', 'Nothing'], correct: 1,
      why: 'The first lab block builds a two-VLAN network from a plan, and the plan is the one you made last session.',
      fix: 'Find your Session 4 notes. If you did not make an address plan, make one now: it takes twenty minutes.' },
  ],
  6: [
    { q: 'A fixture at address 21 using 16 channels means the next fixture starts at:', options: ['22', '36', '37', '41'], correct: 2,
      why: 'Address plus footprint: 21 plus 16 is 37. The commonest slip is adding one instead of the footprint.',
      fix: 'Address a real fixture by hand before you arrive and calculate what the next one must be.',
      to: { route: '/session/6', id: 'dmx512-a-on-the-wire' } },
    { q: 'A DMX channel value is:', options: ['0 to 127', '0 to 255', '1 to 512', '0 to 512'], correct: 1,
      why: 'One byte per slot. 512 is the number of slots in a universe, not the value range, and confusing the two is common.',
      fix: 'Reread bits and bytes. A slot is a position; its value is a byte.',
      to: { route: '/foundations', id: 'bits-and-bytes' } },
    { q: 'Why do sACN and Dante use UDP rather than TCP?', options: ['UDP is faster to configure', 'Retransmitting stale real-time data is worse than losing it', 'TCP does not support multicast well and nothing else matters', 'UDP is more reliable'], correct: 1,
      why: 'A lighting level from 40 ms ago is not worth having: a fresh one is on its way. Streaming protocols handle loss by repeating current state.',
      fix: 'Reread the TCP and UDP comparison. It decides the design of every protocol in this session.',
      to: { route: '/session/4', id: 'tcp-udp-and-ports' } },
    { q: 'IGMP snooping without a querier means:', options: ['Multicast is blocked', 'It works, then degrades after a few minutes', 'Nothing changes', 'The switch reboots'], correct: 1,
      why: 'Memberships expire after about 260 to 300 seconds and the switch reverts to flooding, so it works during the check and fails during the show.',
      fix: 'This is Session 5 material and Session 6 assumes it. Reread the IGMP section.',
      to: { route: '/session/5', id: 'three-ways-to-address-a-frame' } },
    { q: 'What must you bring to Session 6?', options: ['A soldering iron', 'Laptop with Wireshark, a DMX cable you trust and one you do not', 'A console', 'Nothing'], correct: 1,
      why: 'One lab block is on a scope with real DMX, and the untrusted cable is the point of the exercise.',
      fix: 'Read the preparation block, and find both a terminated and an unterminated DMX line in your venue.' },
  ],
  7: [
    { q: 'byte AND 0x80 tests:', options: ['Whether the byte is even', 'Whether the top bit is set', 'Whether the byte is above 80', 'The low nibble'], correct: 1,
      why: '0x80 is 1000 0000, so the AND keeps only the top bit. In MIDI, a non-zero result means this is a status byte.',
      fix: 'Reread the bitwise operations. This one test is most of what makes MIDI readable.',
      to: { route: '/session/2', id: 'the-numbers-everything-is-written-in' } },
    { q: '0x7F in decimal is:', options: ['79', '127', '128', '255'], correct: 1,
      why: 'The largest MIDI data byte, and one below the smallest status byte. Both numbers are worth knowing on sight.',
      fix: 'Drill the hex table. This session builds messages in hex by hand.',
      to: { route: '/foundations', id: 'hexadecimal' } },
    { q: 'A command differs from data because:', options: ['It is faster', 'It is an event that happens once and is not recoverable if lost', 'It is bigger', 'It uses TCP'], correct: 1,
      why: 'Data is state and is usually repeated, so a lost copy repairs itself. A command is an event: if it is missed, nothing recovers it.',
      fix: 'Reread the commands and data section of Session 1. It is the distinction this whole session turns on.',
      to: { route: '/session/1', id: 'commands-data-and-the-difference' } },
    { q: 'To send a UDP packet to a program on another machine you need:', options: ['Its MAC address', 'Its IP address and a port number', 'A TCP connection', 'Its hostname only'], correct: 1,
      why: 'The address gets you to the machine and the port to the program. Together they are a socket.',
      fix: 'Reread ports and sockets. Every OSC integration in this session is an address and a port.',
      to: { route: '/session/4', id: 'tcp-udp-and-ports' } },
    { q: 'What must you bring to Session 7?', options: ['A MIDI keyboard', 'A laptop with a MIDI monitor and either QLab, Companion or an OSC tool installed', 'A DMX cable', 'Nothing'], correct: 1,
      why: 'Both lab blocks build cue paths on your own machine, and installing software in the room wastes the block.',
      fix: 'Install a MIDI monitor, send it something, and watch bytes appear. That is the whole first half of the session in one exercise.' },
  ],
  8: [
    { q: 'A frame at 25 fps lasts:', options: ['25 ms', '33.37 ms', '40 ms', '41.67 ms'], correct: 2,
      why: 'One divided by 25 is 40 milliseconds. At 29.97 it is 33.37 ms and at 24 it is 41.67 ms.',
      fix: 'Practise reciprocals of frame rates. Every drop-frame calculation in this session starts there.',
      to: { route: '/field', id: 'timecode' } },
    { q: 'PTP is needed rather than NTP because:', options: ['It is newer', 'Media over IP needs sub-microsecond agreement, not millisecond', 'NTP does not work on a LAN', 'It uses less bandwidth'], correct: 1,
      why: 'NTP is good to milliseconds, which is fine for logs and useless for lining up audio samples taken in different boxes.',
      fix: 'Reread the time section of Session 5, particularly the four-message exchange and the failure modes.',
      to: { route: '/session/5', id: 'time-on-the-network' } },
    { q: 'An OSC address pattern looks like:', options: ['A hex byte', '/cue/12.5/go', 'OSC:12.5:GO', 'A JSON object'], correct: 1,
      why: 'A string beginning with a slash, structured like a filesystem path, with a comma-led type tag and typed arguments after it.',
      fix: 'Send one OSC message to something before you arrive. QLab, a console with OSC enabled, or a tool talking to itself.' },
    { q: 'A safety function such as an emergency stop belongs:', options: ['On its own VLAN', 'On the control VLAN with QoS', 'Hardwired or on a certified safety bus, not on the show network', 'On a wireless link'], correct: 2,
      why: 'It must work when everything else has failed, which is exactly when a shared best-effort network is least trustworthy. It is a certification question, not a performance one.',
      fix: 'Reread the safety principle. You will be in meetings where somebody proposes the first two options.',
      to: { route: '/principles', id: '1-ensure-safety' } },
    { q: 'What must you bring to Session 8?', options: ['Nothing', 'Your one-page drawing of a production’s control system, and everything from the module', 'A fixture', 'A switch'], correct: 1,
      why: 'The final lab block is a design exercise using all of it, and it starts from the drawing you brought.',
      fix: 'Draw one production’s control system on one page and label every line with its protocol. The lines you cannot label are the interesting ones.' },
  ],
};

// ---------------------------------------------------------------------------
// Fault diagnosis scenarios
//
// Scored on the order you investigate in. Each step has a time cost and a flag
// saying whether it could have narrowed the boundary at all: a test that could
// not have changed your mind is a wasted test, however interesting the result.
// ---------------------------------------------------------------------------

export const faultScenarios = [
  {
    cls: 4,
    symptom: 'A new lighting node will not appear on the console, and its own web interface will not load.',
    context: 'It has a link light. It is set to DHCP. Every other device on the switch is fine.',
    steps: [
      { text: 'Read the node’s display or use the manufacturer tool to see its address', cost: 3, good: true, result: 'It reads 169.254.11.4. It asked for an address and nothing answered.' },
      { text: 'Check whether other devices on that switch got DHCP addresses', cost: 3, good: true, result: 'They did. So the DHCP server is alive and reachable from that switch.' },
      { text: 'Replace the node', cost: 25, good: false, result: 'The replacement does exactly the same thing. The node was not the fault.' },
      { text: 'Check which VLAN the node’s switch port is in', cost: 4, good: true, result: 'Port 14 is in VLAN 1, not VLAN 10. It is on a network with no DHCP server.' },
      { text: 'Replace the patch cable', cost: 5, good: false, result: 'No change, and the link light already told you layer 1 was fine.' },
      { text: 'Ping the node', cost: 2, good: false, result: 'No reply, which you already knew from the console not seeing it.' },
      { text: 'Restart the DHCP server', cost: 10, good: false, result: 'Every other device loses and regains its lease. The node is unchanged, and you have disturbed a working system.' },
      { text: 'Set the node to a static address in the right range and retest', cost: 5, good: true, result: 'Still nothing, which confirms the problem is the port rather than the addressing.' },
    ],
    cause: 'The switch port was left in the default VLAN rather than being put in the lighting VLAN, so the node was on a network with no DHCP server and no console.',
    fix: 'Move port 14 to VLAN 10, set the node back to DHCP, and confirm it receives an address in the lighting range.',
    prevent: 'Configure every unused port into an unused VLAN by default, and keep a port map so that "which VLAN is this port in" is a lookup rather than an investigation.',
  },
  {
    cls: 5,
    symptom: 'A rig is proved perfect at 14:00. At 20:15 during the show, fixtures across several universes stutter and a media server drops frames.',
    context: 'Forty universes of sACN, three managed switches, nothing was changed between the check and the show.',
    steps: [
      { text: 'Capture on a mirror port and run Statistics, Protocol Hierarchy', cost: 5, good: true, result: 'Multicast is 94 per cent of traffic and it is reaching a port with no lighting device on it.' },
      { text: 'Filter igmp and look for queries', cost: 4, good: true, result: 'Joins and leaves are present. There are no queries at all, from anything.' },
      { text: 'Reboot the console', cost: 8, good: false, result: 'The rig comes back, works for four minutes, and degrades again. You have reproduced the fault without explaining it.' },
      { text: 'Check IGMP snooping is enabled on each switch', cost: 5, good: true, result: 'Enabled on all three. Snooping is on and there is nobody to keep the memberships alive.' },
      { text: 'Replace the uplink fibre', cost: 20, good: false, result: 'No change. The port counters showed no errors, which you could have read first.' },
      { text: 'Read the switch port error counters', cost: 4, good: true, result: 'Clean. No CRC errors anywhere, so this is not a physical fault.' },
      { text: 'Move the media server to a different switch', cost: 12, good: false, result: 'It still drops frames, because it is still receiving forty universes it never asked for.' },
      { text: 'Enable an IGMP querier on the core switch for the lighting VLAN', cost: 5, good: true, result: 'Queries appear. Memberships stop expiring, multicast returns to the ports that asked for it, and the traffic falls by 90 per cent.' },
    ],
    cause: 'IGMP snooping was enabled with no querier anywhere. Group memberships aged out after about five minutes and every switch reverted to flooding all forty universes to every port.',
    fix: 'Enable exactly one IGMP querier per VLAN, normally on the core switch, and confirm queries are visible in a capture.',
    prevent: 'Prove the rig, leave it untouched for ten minutes, and prove it again. That single habit catches this before a show does.',
  },
  {
    cls: 5,
    symptom: 'Audio glitches every few minutes. Nothing else on the network is affected and nothing was changed.',
    context: 'A Dante system on its own VLAN, two mixing consoles, a Dante-enabled amplifier rack and a laptop running Dante Controller.',
    steps: [
      { text: 'Filter ptp in a capture and count distinct grandmaster identities', cost: 5, good: true, result: 'Two identities alternate in the announce messages, minutes apart.' },
      { text: 'Read the PTP priority settings on both consoles', cost: 4, good: true, result: 'Both are at the default priority. Neither is configured as the preferred grandmaster.' },
      { text: 'Replace the audio patch cables', cost: 15, good: false, result: 'No change. The glitch is not a physical fault and the port counters were clean.' },
      { text: 'Check whether PTP is crossing a router', cost: 3, good: true, result: 'It is not. Everything is in one VLAN, which is correct.' },
      { text: 'Increase the Dante latency setting', cost: 5, good: false, result: 'The glitches are less frequent and still present. You have masked a symptom.' },
      { text: 'Reboot the amplifier rack', cost: 8, good: false, result: 'No change, and the rack was never the source of the clock.' },
      { text: 'Set console A as the preferred grandmaster and console B as backup', cost: 5, good: true, result: 'The election settles. One identity in the announce messages, and the glitches stop.' },
      { text: 'Check every device is on the same PTP profile and domain', cost: 4, good: true, result: 'All on the same profile and domain, which rules out the other common cause.' },
    ],
    cause: 'Two devices at equal PTP priority kept re-electing each other as grandmaster, and each election stepped the clock.',
    fix: 'Set one device as the preferred grandmaster and one as backup, with explicit priorities, and confirm one stable identity in the announce messages.',
    prevent: 'Name the grandmaster on the system drawing. It is one of the three singular roles on a show network, alongside the DHCP server and the IGMP querier.',
  },
  {
    cls: 6,
    symptom: 'One fixture halfway along a run flickers. The ones after it are fine.',
    context: 'Six fixtures on one 90 m DMX run. It started after another 20 m was added at the far end.',
    steps: [
      { text: 'Check the terminator is present at the new far end', cost: 3, good: true, result: 'It is still on the fixture that used to be last, not on the new one. The last 20 m is an unterminated stub.' },
      { text: 'Swap the flickering fixture', cost: 15, good: false, result: 'The new one flickers in the same position. The fault follows the position, not the fixture.' },
      { text: 'Scope the line at the far end', cost: 8, good: true, result: 'Substantial ringing after each edge, and the timing puts the reflection about 20 m away.' },
      { text: 'Check the fixture’s address', cost: 3, good: false, result: 'Correct. Addressing does not cause flicker, it causes the wrong thing to move.' },
      { text: 'Move the terminator to the actual far end', cost: 3, good: true, result: 'The ringing collapses and the flicker stops.' },
      { text: 'Replace the whole run with new cable', cost: 45, good: false, result: 'It works, and you have spent an hour and learned nothing about why.' },
      { text: 'Reduce the console’s channel count to speed up the refresh', cost: 5, good: false, result: 'The flicker is less noticeable. You have made a fault harder to see.' },
      { text: 'Count the devices on the segment', cost: 3, good: true, result: 'Six. Well under 32, so unit loading is not the cause.' },
    ],
    cause: 'The terminator was left at the old end of the run, so the added 20 m was an unterminated stub reflecting every edge back into the line.',
    fix: 'Move the terminator to the physical far end and confirm on a scope that the ringing is gone.',
    prevent: 'Make "the terminator moves when the run moves" part of the change procedure, and scope the far end after any change to a DMX run.',
  },
  {
    cls: 6,
    symptom: 'RDM discovers nothing, although all twenty fixtures respond to DMX perfectly.',
    context: 'Console, one opto-splitter feeding four DMX lines, twenty RDM-capable fixtures.',
    steps: [
      { text: 'Check that RDM is enabled on the console output', cost: 3, good: true, result: 'It is enabled and the console reports it is sending discovery.' },
      { text: 'Read the splitter’s model number and check whether it passes RDM', cost: 4, good: true, result: 'It is a DMX-only opto-splitter. It regenerates the forward direction and blocks the return.' },
      { text: 'Update the fixtures’ firmware', cost: 40, good: false, result: 'No change, and you have spent an afternoon on twenty fixtures that were never the problem.' },
      { text: 'Connect one fixture directly to the console and discover', cost: 6, good: true, result: 'It discovers instantly. The fixtures are fine and the fault is between them and the console.' },
      { text: 'Replace the DMX cable to the first fixture', cost: 6, good: false, result: 'No change. DMX was flowing perfectly, so the forward path was never in doubt.' },
      { text: 'Check the terminators', cost: 4, good: false, result: 'All present. Termination affects signal quality, not the direction of the conversation.' },
      { text: 'Bypass the splitter for one line and discover again', cost: 6, good: true, result: 'All five fixtures on that line discover. The splitter is confirmed.' },
      { text: 'Change every fixture’s address by hand from the ladder', cost: 90, good: false, result: 'It works. It is also the entire day of work that RDM exists to avoid.' },
    ],
    cause: 'A DMX-only opto-splitter in the path blocks the RDM return direction while passing DMX perfectly, so every fixture works and none is discoverable.',
    fix: 'Replace the splitter with an RDM-capable one, or route RDM around it, and rediscover.',
    prevent: 'Specify RDM-capable splitters and nodes as standard, and test discovery end to end during the rig check rather than during the show.',
  },
  {
    cls: 4,
    symptom: 'Some devices work and some do not, and which ones changes every time anything is power cycled.',
    context: 'A fit-up week. Several contractors have been on site. The network was fine on Monday.',
    steps: [
      { text: 'Filter dhcp in a capture and count who answers an offer', cost: 5, good: true, result: 'Two servers answer. One is offering 10.101.10.x, the other 192.168.1.x with a different gateway.' },
      { text: 'Compare the addresses of a working and a non-working device', cost: 4, good: true, result: 'The working ones are on 10.101.10.x. The failing ones are on 192.168.1.x.' },
      { text: 'Reboot the switch', cost: 10, good: false, result: 'Everything renegotiates and a different set of devices now works. You have reshuffled the fault.' },
      { text: 'Trace the second server’s MAC to a switch port', cost: 8, good: true, result: 'Port 22. Following the cable finds a small travel router under a production desk.' },
      { text: 'Set every device to a static address', cost: 60, good: false, result: 'It works, and you have spent an hour doing by hand what removing one box would have fixed.' },
      { text: 'Check the port error counters', cost: 4, good: false, result: 'Clean. This was never a physical fault.' },
      { text: 'Remove the travel router and release and renew the affected devices', cost: 8, good: true, result: 'One DHCP server answers. Every device comes back on the right range.' },
      { text: 'Ask what changed since Monday', cost: 2, good: true, result: '"Somebody plugged something in to charge a laptop." That is the whole answer, in one question.' },
    ],
    cause: 'A second DHCP server, on a contractor’s travel router, handing out addresses on a different range with a different gateway to whichever devices asked it first.',
    fix: 'Remove it, then release and renew every affected device so it takes a lease from the correct server.',
    prevent: 'DHCP snooping on the switches so only the authorised server can offer leases, unused ports shut down, and a rule that nothing is plugged into a show network without being asked for.',
  },
  {
    cls: 7,
    symptom: 'A cue from the lighting console to the playback machine fires perhaps nine times out of ten.',
    context: 'MSC over a DIN MIDI cable, about 25 m through a patch panel. It has been like this since the fit-up.',
    steps: [
      { text: 'Put a MIDI monitor on the receiving end and watch across twenty cues', cost: 8, good: true, result: 'Two of the twenty messages arrive truncated: the SysEx ends without an F7.' },
      { text: 'Replace the playback machine', cost: 40, good: false, result: 'Same behaviour. The message was already damaged before it arrived.' },
      { text: 'Check the cable length against the specification', cost: 3, good: true, result: '25 m through a patch panel, with 15 m the usual practical limit for DIN MIDI.' },
      { text: 'Add a delay between cues on the console', cost: 5, good: false, result: 'Slightly better, which is a coincidence of timing rather than a fix.' },
      { text: 'Bypass the patch panel with a single 5 m cable and retest', cost: 8, good: true, result: 'Twenty out of twenty arrive intact. The fault is the run, not the ends.' },
      { text: 'Reload the show file on the console', cost: 10, good: false, result: 'No change. The console was sending correctly all along, which the monitor showed.' },
      { text: 'Move the cue path to RTP-MIDI over the control VLAN', cost: 15, good: true, result: 'Twenty out of twenty, repeatably, and the distance problem disappears entirely.' },
      { text: 'Add a contact closure alongside for the one critical cue', cost: 10, good: true, result: 'Now the show-stopping cue has a path with no dependencies at all.' },
    ],
    cause: 'A DIN MIDI run well beyond its practical length, through a patch panel, corrupting occasional SysEx messages so they were discarded as malformed.',
    fix: 'Carry MSC over RTP-MIDI on the control VLAN, or shorten the run and remove the patch panel from the path.',
    prevent: 'Treat DIN MIDI as a short-run interface. For anything crossing a building, use RTP-MIDI, and put a contact closure alongside the one cue that must not fail.',
  },
  {
    cls: 8,
    symptom: 'Video and lighting start together and are visibly apart by the end of a fifty minute show.',
    context: 'Timecode from a playback machine to a lighting console and a video server. It has always done this.',
    steps: [
      { text: 'Read the frame rate setting on all three devices', cost: 4, good: true, result: 'The playback machine and the video server are 29.97 drop frame. The lighting console is 29.97 non-drop.' },
      { text: 'Calculate the expected drift over fifty minutes', cost: 3, good: true, result: '3.6 seconds an hour is about 3 seconds over fifty minutes, which matches what the room is seeing.' },
      { text: 'Replace the timecode cable', cost: 8, good: false, result: 'No change. The signal was arriving perfectly; it was being counted differently.' },
      { text: 'Add an offset to the lighting cues to compensate', cost: 20, good: false, result: 'It matches at one point in the show and is wrong everywhere else, because the error accumulates.' },
      { text: 'Check for a second timecode source', cost: 5, good: false, result: 'Only one generator. This is not a two-source problem.' },
      { text: 'Set the lighting console to 29.97 drop frame and re-run', cost: 4, good: true, result: 'The three stay together for the whole show.' },
      { text: 'Reboot the video server', cost: 8, good: false, result: 'No change. Its setting was correct.' },
      { text: 'Write the frame rate on the system drawing and in the show file notes', cost: 3, good: true, result: 'The next person to touch this now cannot make the same mistake.' },
    ],
    cause: 'One device counting non-drop while the others counted drop frame, so it fell 3.6 seconds per hour behind while every device reported healthy timecode.',
    fix: 'Set every device on the chain to the same frame rate and drop-frame setting, and prove it over a full show length.',
    prevent: 'Put the frame rate and the drop-frame setting on the system drawing. It is one setting, on one menu page, on every device, and it is invisible until it has accumulated.',
  },
  {
    cls: 2,
    symptom: 'A GPI-triggered effect fires twice, occasionally, and only during performances.',
    context: 'A stage manager’s button on 30 m of cable to a show controller’s contact input. It never does it in a rehearsal.',
    steps: [
      { text: 'Log the input with timestamps across a run', cost: 8, good: true, result: 'The double fires are 6 to 14 ms apart, which is a bounce window rather than two presses.' },
      { text: 'Replace the button', cost: 12, good: false, result: 'Still happens. Every mechanical contact bounces; the button was normal.' },
      { text: 'Check whether the input has a debounce setting', cost: 4, good: true, result: 'It has one, set to 0 ms. Its default was changed during commissioning.' },
      { text: 'Ask what is different about a performance', cost: 3, good: true, result: 'The stage manager presses harder and faster under pressure, which makes the bounce worse.' },
      { text: 'Replace the cable', cost: 15, good: false, result: 'No change. A 6 ms double is a bounce signature, not a noise signature.' },
      { text: 'Set the debounce to 30 ms and retest', cost: 4, good: true, result: 'No double fires across a hundred presses, including deliberately violent ones.' },
      { text: 'Add a capacitor across the input', cost: 10, good: false, result: 'It also works, and it is a hardware change where a setting existed.' },
      { text: 'Check the input has a pull-up', cost: 4, good: true, result: 'It does. A floating input would have fired at random rather than in pairs, so this was the right thing to rule out.' },
    ],
    cause: 'Contact bounce with the input’s debounce filter set to zero, so a single hard press was read as two closures.',
    fix: 'Set the debounce window to 30 ms and prove it against a hundred deliberately hard presses.',
    prevent: 'Test every operator input the way an operator will actually use it, under pressure, rather than the way a technician tests it.',
  },
];

// ---------------------------------------------------------------------------
// Decode the bytes
//
// Reading a protocol out of raw hex. The point is fluency: a technician who can
// look at a capture and say what it is has a diagnostic advantage that no tool
// provides.
// ---------------------------------------------------------------------------

export const decodeChecks = [
  { tag: 'Numbers', q: '`0xB7` in binary and decimal.', a: '`1011 0111`, which is 183. Two nibbles: B is 1011, 7 is 0111.', note: 'One hex digit is exactly four bits, so conversion is a lookup rather than arithmetic.' },
  { tag: 'Numbers', q: '`0xFF` as an unsigned byte, and as a signed one.', a: '255 unsigned, −1 as signed two’s complement. Same eight bits, different agreement.', note: 'A value that jumps to 255 where you expected −1 is a signedness bug.' },
  { tag: 'Numbers', q: 'The bytes `01 2C` read as a big-endian 16-bit value.', a: '300. The most significant byte comes first, which is network byte order.', note: 'Read little-endian it would be 11265. Universe 1 becoming universe 256 is this bug.' },
  { tag: 'Numbers', q: 'What `192 & 224` gives, in binary and decimal.', a: '`1100 0000 & 1110 0000` = `1100 0000`, which is 192. The AND keeps only bits set in both.', note: 'This is a mask being applied. It is also every subnet calculation.' },
  { tag: 'Numbers', q: 'The BCD byte `0x47` as a decimal number.', a: '47. Each nibble holds one decimal digit, so 4 and 7.', note: 'Read as ordinary binary it would be 71. BCD survives in timecode and in older industrial gear.' },

  { tag: 'DMX', q: 'A DMX start code of `0x00`.', a: 'Null start code: ordinary dimmer data follows, up to 512 slots.', note: 'Any other start code must be ignored by a device that does not understand it.' },
  { tag: 'DMX', q: 'A DMX start code of `0xCC`.', a: 'RDM. The message that follows is a device management request or response, sharing the line with the lighting data.', note: 'A fixture that misbehaves when RDM is enabled has a firmware bug, and it is a known category.' },
  { tag: 'DMX', q: 'A line held low for 100 µs on a DMX pair.', a: 'A break. It marks the start of a packet, and it is deliberately longer than any valid byte so it cannot be mistaken for data.', note: 'The minimum is 92 µs, followed by a mark of at least 12 µs.' },
  { tag: 'DMX', q: 'A universe refreshing every 11.4 ms.', a: 'About 88 Hz, so roughly 256 channels rather than a full 512.', note: 'Fewer channels refresh faster. It is a real effect on fast moving-light chases.' },
  { tag: 'DMX', q: 'A fixture at address 470 with a 48-channel personality.', a: 'It does not fit. 470 + 48 − 1 = 517, past the 512th slot, so it will behave strangely rather than refusing.', note: 'The 513th byte does not exist. Check the footprint against the top of the universe.' },

  { tag: 'sACN', q: 'The multicast group for sACN universe 1.', a: '`239.255.0.1`. The last two octets are the universe as a 16-bit number.', note: 'Universe divided by 256 is the third octet; the remainder is the fourth.' },
  { tag: 'sACN', q: 'The multicast group for sACN universe 300.', a: '`239.255.1.44`. 300 = 256 + 44.', note: 'This is the arithmetic behind a Wireshark filter that isolates one universe out of a hundred.' },
  { tag: 'sACN', q: 'Traffic on UDP port 5568.', a: 'sACN, E1.31. Streaming lighting data over multicast.', note: 'Port 6454 would be Art-Net, which broadcasts by default.' },
  { tag: 'sACN', q: 'An sACN packet with priority 200 arriving alongside one at 100.', a: 'The priority 200 source wins outright for that universe. There is no merging between different priorities.', note: 'Which is why 200 is the wrong default for a main console: nothing can then override you.' },
  { tag: 'sACN', q: 'A receiver that has heard nothing from a source for 3 seconds.', a: 'Past the 2.5 s source timeout. The source is considered gone, and what happens to the levels next is a device setting.', note: 'Hold last look, fade out or go dark. Find out which, for your fixtures, and write it down.' },

  { tag: 'IP', q: '`10.101.7.150/26`: the network address.', a: '`10.101.7.128`. Block size is 256 − 192 = 64, and 150 falls in the third block.', note: 'Usable range .129 to .190, broadcast .191.' },
  { tag: 'IP', q: 'A device with the address `169.254.31.7`.', a: 'Link-local. It is set to DHCP, it asked, and nothing answered.', note: 'A diagnosis rather than a configuration. Two such devices can still talk to each other, which is the confusing part.' },
  { tag: 'IP', q: 'A destination MAC of `FF:FF:FF:FF:FF:FF`.', a: 'Broadcast. Every device on the broadcast domain must receive and process it.', note: 'ARP and DHCP discovery are the two you will see constantly.' },
  { tag: 'IP', q: 'A destination MAC beginning `01:00:5E`.', a: 'IPv4 multicast. The low 23 bits are derived from the group address.', note: 'Whether the switch forwards it only where wanted depends entirely on IGMP snooping.' },
  { tag: 'IP', q: 'The mask `255.255.254.0` in CIDR notation.', a: '/23. 255.255 is 16 bits, 254 is `11111110`, which is 7 more.', note: 'A /23 holds 510 usable addresses, which is the right size for a large pixel rig.' },

  { tag: 'MIDI', q: '`0x90 0x3C 0x64`', a: 'Note On, channel 1, note 60 which is middle C, velocity 100.', note: 'The high nibble 9 is the message type and the low nibble 0 is the channel, displayed to humans as 1.' },
  { tag: 'MIDI', q: '`0xC0 0x07`', a: 'Program Change, channel 1, program 7. The most common way a show cue is recalled over plain MIDI.', note: 'Two bytes: 128 programs per channel, 16 channels, so 2048 distinct triggers.' },
  { tag: 'MIDI', q: 'A byte of `0x45` arriving with no status byte before it.', a: 'A data byte, 69, belonging to whatever message last set the running status.', note: 'Running status omits a repeated status byte. It is why a MIDI parser must be a state machine.' },
  { tag: 'MIDI', q: '`0xFE` arriving about every 300 ms.', a: 'Active sensing. A keep-alive: if the receiver has seen one and then hears nothing for about 300 ms, it silences everything.', note: 'It fills a MIDI monitor with noise and it is a 1983 protocol answering the fourth question properly.' },
  { tag: 'MIDI', q: 'Why a MIDI data value never exceeds 127.', a: 'The top bit distinguishes a status byte from a data byte, so data has seven bits available.', note: '`byte & 0x80` is the whole test, and it makes the protocol readable.' },

  { tag: 'MSC', q: '`F0 7F 01 02 01 01 F7`', a: 'MSC: device 1, lighting, GO, with no cue number, which means the next cue.', note: 'F0 opens SysEx, 7F is universal real-time, 02 is the MSC sub-ID.' },
  { tag: 'MSC', q: '`F0 7F 03 02 10 02 F7`', a: 'MSC: device 3, sound general, STOP.', note: 'Command format 0x10 is sound; 0x01 is lighting and 0x20 is machinery.' },
  { tag: 'MSC', q: 'The bytes `31 32 2E 35` in an MSC data field.', a: 'ASCII "12.5", the cue number. 31 is "1", 32 is "2", 2E is ".", 35 is "5".', note: 'Cue number, cue list and cue path are ASCII, separated by 0x00.' },
  { tag: 'MSC', q: 'An MSC device ID of `0x7F`.', a: 'All-call. Every device on the line acts on it.', note: 'The command format still filters by discipline, so a lighting all-call does not fire the sound cues.' },

  { tag: 'Time', q: '`01:23:45;12` rather than `01:23:45:12`.', a: 'The semicolon marks drop-frame timecode, so this is 29.97 fps counting that skips numbers to match the wall clock.', note: 'There is no drop frame at 25 fps. A device offering it has been misconfigured.' },
  { tag: 'Time', q: 'Two timecode sources 3.6 seconds apart after one hour, both reporting healthy.', a: 'One is drop frame and one is non-drop at 29.97 fps.', note: 'The classic long-show drift, and it is one menu setting on one device.' },
  { tag: 'Time', q: 'Eight consecutive `F1` messages on a MIDI line.', a: 'MIDI Time Code quarter frames: one nibble each, so eight of them carry one complete timecode value across two frames.', note: 'Which is why MTC is inherently two frames behind.' },
  { tag: 'Time', q: 'Traffic on UDP ports 319 and 320.', a: 'PTP: 319 carries the event messages that are timestamped, 320 the general ones.', note: 'Count distinct grandmaster identities in the announce messages when audio is glitching.' },

  { tag: 'OSC', q: '`/eos/chan/1/level` with a type tag of `,f` and an argument of 75.0', a: 'An OSC message setting channel 1 to 75 per cent on an ETC Eos console.', note: 'The address is a string and the type tag says the argument is a 32-bit float.' },
  { tag: 'OSC', q: 'An OSC packet beginning `#bundle`.', a: 'A bundle: a 64-bit time tag followed by several messages, to be executed together at that moment.', note: 'A time tag of 1 means immediately, and most implementations support nothing else.' },
];
